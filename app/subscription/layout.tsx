import type { Metadata } from "next"
export const metadata: Metadata = {
  title: "Subscription Plans",
  description: "Choose a MyScriptic subscription plan for unlimited access to thousands of eBooks and audiobooks. Start your free trial today.",
}
export default function SubscriptionLayout({ children }: { children: React.ReactNode }) { return children }
